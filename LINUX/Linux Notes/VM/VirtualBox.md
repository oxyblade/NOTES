### *INSTALL*

https://www.virtualbox.org/wiki/Linux_Downloads
All distributions (built on EL6 and therefore not requiring recent system libraries) -> Download file *.run

```
sudo usermod -aG vboxusers $USER
sudo /sbin/vboxconfig
```